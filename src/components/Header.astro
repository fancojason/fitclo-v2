---
const navItems = [
  { name: 'PRODUCTS', href: '/products' },
  { name: 'READY TO SHIP', href: '/ready-to-ship' },
  { name: 'PRIVATE LABEL', href: '/private-label' },
  { name: 'MANUFACTURING', href: '/capabilities' },
];
---

<header class="fixed w-full z-50 bg-black/90 backdrop-blur-md border-b border-white/10 py-2">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex-shrink-0 flex items-center">
        <a href="/" class="flex items-center space-x-3 group">
          <div class="flex items-baseline font-black text-2xl tracking-tighter">
            <span class="text-white">F</span>
            <span class="text-accent">C</span>
          </div>
          <div class="text-xl font-bold tracking-[0.25em] flex uppercase">
            <span class="text-white">FIT</span>
            <span class="text-accent">CLO</span>
          </div>
        </a>
      </div>
      <nav class="hidden md:flex space-x-10">
        {navItems.map(item => (
          <a href={item.href} class="text-[13px] font-semibold text-gray-300 hover:text-accent transition-colors tracking-[0.15em]">
            {item.name}
          </a>
        ))}
      </nav>
      <div class="md:hidden">
        <!-- Mobile menu button -->
      </div>
    </div>
  </div>
</header>
